import type { PrBuildrConfig } from "../config/schema.js";
import type { AIProvider } from "./types.js";

/**
 * Create an AI provider instance based on the current config.
 */
export function createAIProvider(_config: PrBuildrConfig): AIProvider {
  // Stub — implementation in Phase 3
  throw new Error("AI provider not yet implemented");
}
