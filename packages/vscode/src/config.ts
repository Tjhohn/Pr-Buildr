/**
 * VS Code settings integration for pr-buildr.
 *
 * Bridges VS Code configuration with @pr-buildr/core config.
 */
export function getVSCodeConfig(): { provider?: string; model?: string; token?: string } {
  // Stub — implementation in Phase 5
  return {};
}
