/**
 * Terminal output formatting and display helpers.
 */

/**
 * Display the generated draft in the terminal.
 */
export function displayDraft(_title: string, _body: string): void {
  // Stub — implementation in Phase 4
}

/**
 * Display the PR creation result.
 */
export function displayResult(_url: string, _draft: boolean): void {
  // Stub — implementation in Phase 4
}
