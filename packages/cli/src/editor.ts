/**
 * Open a temporary file in the user's $EDITOR for draft editing.
 * Returns the edited content.
 */
export async function openInEditor(
  _title: string,
  _body: string,
): Promise<{ title: string; body: string }> {
  // Stub — implementation in Phase 4
  throw new Error("Editor integration not yet implemented");
}
